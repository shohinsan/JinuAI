# Database Configuration
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=banana

# Application Security
SECRET_KEY=your_secret_key_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Google AI Configuration
GOOGLE_API_KEY=your_google_api_key_here

# MinIO Object Storage Configuration
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=jinuai-assets
MINIO_SECURE=false  # Set to true if using HTTPS

# Optional: MinIO Prefix Configuration (defaults are fine for most cases)
# MINIO_PREFIX_MEDIA=media
# MINIO_PREFIX_MODELS=models
# MINIO_PREFIX_STYLES=styles

# CORS Configuration
# BACKEND_CORS_ORIGINS=["http://localhost:3000"]
# FRONTEND_HOST=http://localhost:3000

# Redis Configuration (Optional)
# REDIS_URL=redis://localhost:6379

# Observability (Optional - only needed for production)
# SENTRY_DSN=your_sentry_dsn_here
# LOGFIRE_TOKEN=your_logfire_token_here
